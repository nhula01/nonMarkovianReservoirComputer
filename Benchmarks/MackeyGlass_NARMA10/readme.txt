tau = 10 # Delay
delay_steps = tau
t_renew = 3 # inject constant input for t times
t_max = 1000 * t_renew # Total Time Evolution
Delta = 0 # Detuning
gamma = .1 # Decaying
omega = .15 # input strength
phi = np.pi/3 # phase
max_photon = 3 # amount of photons
max_bin = 50 # Maximal bins
delta_t = 1 # system time step
chi_max = 10

we change omega/epsilon as [.08, .1,.12,.14, .16] 

Note: omega and epsilon are used interchangeable 

